<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:baselineAligned="false"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="@dimen/widget_height"
    android:elevation="4dp">

    <LinearLayout android:id="@+id/quote"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight=".8"
        android:orientation="vertical"

        android:layout_marginLeft="@dimen/quote_horizontal_margin"
        android:paddingLeft="@dimen/quote_horizontal_padding"
        android:layout_marginRight="@dimen/quote_horizontal_margin"
        android:paddingRight="@dimen/quote_horizontal_padding"
        android:layout_marginTop="@dimen/quote_vertical_margin"
        android:paddingTop="@dimen/quote_vertical_padding"
        android:layout_marginBottom="@dimen/quote_vertical_margin"
        android:paddingBottom="@dimen/quote_vertical_padding" >

        <TextView android:id="@+id/quote_text"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight=".75"

            android:textColor="#000000"
            android:textSize="16sp"
            android:text="@string/lipsum"/>

        <TextView android:id="@+id/quote_author"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight=".25"

            android:gravity="end"
            android:text="@string/author_name"/>
    </LinearLayout>

    <LinearLayout
        android:id="@+id/buttons"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight=".2"
        android:orientation="vertical">

        <ImageButton
            android:id="@+id/share_button"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight=".5"
            android:background="@color/colorPrimary"
            android:src="@drawable/ic_share"
            android:elevation="4dp"/>
        <ImageButton
            android:id="@+id/source_button"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight=".5"
            android:background="@color/colorPrimary"
            android:src="@drawable/ic_book"
            android:elevation="4dp"/>
    </LinearLayout>

</LinearLayout>